[Unit]
Description=Check for pacman and AUR updates

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'updates="$(checkupdates; trizen -Sau 2>&1 | grep -v :: | awk \'{print substr($2,1,length($2)-1),$3,"->",$5}\')"; [ -n "$updates" ] && notify-send "updates available" "$updates" || true'
